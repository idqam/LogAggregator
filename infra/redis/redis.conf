maxmemory 512mb
maxmemory-policy noeviction ## this allows us to test the behavior when Redis hits memory limits, which is important for our use case. In production, we would monitor and scale Redis accordingly.
appendonly yes
appendfsync everysec
save 900 1
save 300 10